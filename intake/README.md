# Intake

| File | Purpose |
|------|---------|
| `template.json` | Blank schema — copy to `active.json` |
| `active.json` | Current project requirements (gitignored until filled) |

The onboarding automation writes `active.json`. Do not commit real customer PII without consent; use a private repo or redact before push.
